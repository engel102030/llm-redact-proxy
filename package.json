{
  "name": "llm-redact-proxy",
  "version": "0.1.0",
  "description": "Local secret-redaction egress proxy for LLM API traffic. Zero dependencies.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  }
}
